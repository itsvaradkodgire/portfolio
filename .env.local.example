ADMIN_PASSWORD=your-secure-password-here
JWT_SECRET=your-random-32-char-jwt-secret-here

# Google AI Studio — powers the hero chat AND the AI resume tailoring (free tier).
# Get key at: https://aistudio.google.com/app/apikey
GOOGLE_AI_KEY=your-google-ai-studio-key-here
# Optional: override the tailoring model (defaults to gemini-2.5-flash)
# GEMINI_MODEL=gemini-2.5-flash

# REQUIRED IN PRODUCTION: Vercel Blob persists admin content edits.
# Without this, admin "Save" fails on Vercel because the filesystem is read-only.
# Vercel dashboard → Storage → create a Blob store → connect to the project.
BLOB_READ_WRITE_TOKEN=your-vercel-blob-token-here

# Optional: Vercel KV (rate limiting)
KV_URL=your-vercel-kv-url-here
KV_REST_API_URL=your-vercel-kv-rest-api-url-here
KV_REST_API_TOKEN=your-vercel-kv-rest-api-token-here
KV_REST_API_READ_ONLY_TOKEN=your-vercel-kv-read-only-token-here
